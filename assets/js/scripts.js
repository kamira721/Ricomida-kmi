$(document).ready(function () {
    $('[data-toggle="popover"]').popover();
});
// Función de jQuery para que se activen los popover de los botones que tengan el atributo data-toggle con el valor popover//